.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 2rem;

  &__socials {
    display: flex;
  }
}

.header__btns {
  margin: 0 20px;
  flex: 1;

  @include tablets() {
    display: none;
  }
}

.header__logo {
  font-size: 36px;
  font-weight: 900;
}